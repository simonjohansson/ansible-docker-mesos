- name: install mesos
  apt: pkg=mesos install_recommends=no
  notify:
    - restart mesos-master

- name: put mesos config
  copy: src=zk
        dest=/etc/mesos/zk
  notify:
    - restart mesos-master
